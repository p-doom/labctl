# With [remote], for clusters you import artifacts FROM via
# `labctl import-from-cluster`. The section is ignored when this is the current cluster.

name = "example-remote"

[remote]
ssh_alias = "example-login"
# Fallback when no ssh_alias:
# host     = "example.cluster.org"
# ssh_user = "yourusername"

[filesystem]
runs_base = "/scratch/yourusername/labctl_runs"

[filesystem.artifact_roots]
dataset     = "/scratch/yourusername/datasets"
checkpoint  = "/scratch/yourusername/checkpoints"
eval_result = "/scratch/yourusername/eval_logs"

[filesystem.output_roots]
checkpoint_stream = "/scratch/yourusername/checkpoints"
eval_result       = "/scratch/yourusername/eval_logs"

[repos]
myrepo = "/home/yourusername/repos/myrepo"

[scheduler]
kind = "slurm"

[slurm]
qos = "normal"
gres_gpu_syntax = "gpu:{n}"

# For OTP-gated hosts, put ControlMaster in ~/.ssh/config so one OTP
# covers all subsequent labctl ops in the multiplex window:
#
#   Host example-login
#       HostName        example.cluster.org
#       User            yourusername
#       ControlMaster   auto
#       ControlPath     ~/.ssh/cm-%r@%h:%p
#       ControlPersist  8h
